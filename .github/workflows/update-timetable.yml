name: Update Timetable Daily

on:
  schedule:
    # Run every day at 6:00 AM Malaysia Time (UTC+8, which is 22:00 UTC previous day)
    - cron: '0 22 * * *'
  workflow_dispatch: # Allow manual trigger
  
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  update-timetable:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        
      - name: Set timezone
        run: sudo timedatectl set-timezone Asia/Kuala_Lumpur
        
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          
      - name: Install dependencies
        run: npm install
        
      - name: Generate timetable
        env:
          TARUMT_USERNAME: ${{ secrets.TARUMT_USERNAME }}
          TARUMT_PASSWORD: ${{ secrets.TARUMT_PASSWORD }}
        run: node generateTimetable.js
        continue-on-error: true
        
      - name: Commit and push if changed
        run: |
          git config --global user.name 'GitHub Actions'
          git config --global user.email 'actions@github.com'
          git add timetable.ics
          git diff --quiet && git diff --staged --quiet || (git commit -m "Auto-update timetable $(date +'%Y-%m-%d %H:%M:%S')" && git push)
